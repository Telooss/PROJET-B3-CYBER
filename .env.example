# Configuration du Honeypot HTTP
# Copiez ce fichier vers .env et modifiez selon vos besoins

# Port d'écoute du honeypot
BACKEND_PORT=8080

# Mode Flask (production ou development)
FLASK_ENV=production

# Configuration du logging
LOG_LEVEL=INFO

# Limites de ressources (optionnel)
# MEMORY_LIMIT=512m
# CPU_LIMIT=0.5

# Configuration réseau (optionnel)
# LISTEN_IP=0.0.0.0

# Délai de réponse (secondes)
# MIN_DELAY=0.1
# MAX_DELAY=0.5
